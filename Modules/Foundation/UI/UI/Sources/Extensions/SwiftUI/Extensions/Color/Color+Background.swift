import SwiftUI

extension Color {
  public enum background {
    public static let primary = UIAsset.backgroundPrimary.swiftUIColor
    public static let secondary = UIAsset.backgroundSecondary.swiftUIColor
  }
}
